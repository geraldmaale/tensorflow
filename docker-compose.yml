version: '3.6'
services:
  jupyter-lab:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./workspaces:/workspaces
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]